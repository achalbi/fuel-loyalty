<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark cold-start window (Nayara bg.canvas dark) — prevents the white
         flash on launch for dark-mode users before Compose takes over. -->
    <style name="Theme.AceFuelLoyalty" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">#080F18</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
